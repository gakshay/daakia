<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "eDakia" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <!--
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          -->
          <% if user_signed_in? %>  
        		<%= link_to image_tag("logo.jpg", :size => "139x30"), "/transactions", :class => "brand" %> 
        	<% else %>  
        		<%= link_to image_tag("logo.jpg", :size => "139x30"), "/", :class => "brand" %> 
          <% end %>
          <div class="container nav-collapse">
            <ul class="nav">
              <% if user_signed_in? %> 
                <li><%#= link_to_unless current_user.first_name.blank?, current_user.first_name + " (#{current_user.unread_count}) #{number_to_phone(current_user.mobile)}", edit_user_registration_path do
                  link_to current_user.mobile + " (#{current_user.unread_count})", edit_user_registration_path 
                  end %>
                  <%= link_to current_user.mobile + " (#{current_user.unread_count})", edit_user_registration_path %></li>
                <li><%= link_to "Logout", destroy_user_session_path  %></li>
              <% else %>
                <li><%= render :partial => "home/login"%></li>
                <li><%#= link_to "Login", new_user_session_url  %></li>
                <li><%= link_to "Register", new_user_registration_url  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
      <div style="background: url(/assets/postage.png) repeat-x; height: 6px;"></div>
    </div>

    <div class="container">
      <div class="row-fluid">
        <% if user_signed_in? %>
        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <% if user_signed_in? %> 
                <li><%= link_to "Inbox (#{current_user.unread_count})", transactions_url  %></li>
              <% else %>
                <%#= render :partial => "home/login"%>
              <% end %>
              <li><%= link_to "Send Mail", new_transaction_url  %></li>
              <li><%= link_to "Receive Mail", receive_transactions_url  %></li>   
              <li><%= link_to "Logout", destroy_user_session_path  %></li>           
            </ul>
          </div><!--/.well -->
          <%= image_tag("mascot.jpg", :max_width => "286")%>
        </div><!--/span-->
        <div class="span9 content">
        <% elsif retailer_signed_in? %>
          <div class="span3">
            <div class="well sidebar-nav">
              <ul class="nav nav-list">
                <li><%= link_to "Transactions_log", new_transaction_url  %></li>
                <li><%= link_to "Retailer Logout",  destroy_retailer_session_path %></li>
              </ul>
            </div><!--/.well -->
            <%= image_tag("mascot.jpg", :max_width => "286")%>
          </div><!--/span-->
          <div class="span9 content">
        <% else %>
          <div class="content">
        <% end%>
        
          <% unless notice.blank? %>
            <div class="alert alert-success"><%= notice %></div>
          <% end %>
          <% unless alert.blank? %>
      		  <div class="alert alert-error"><%= alert %></div>
      		<% end %>
          <%= yield %>
        </div>
      </div><!--/row-->
      

      <hr>
      <footer>
        <%= link_to "About US", "#", :class => "badge" %> <%= link_to "How it works", "#", :class => "badge" %>           <%= link_to "FAQ", "#", :class => "badge"  %> <%= link_to "Terms and Conditions", "#", :class => "badge"  %> <%= link_to "Team", "#", :class => "badge"  %>
        <p class="pull-right"><em>Copyright &copy; 2012 eDakia Communications, All rights reserved.</em></p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
